name: pytype check

on:
  push:

jobs:
  check:
    uns-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: pytypes checker
      uses: theahura/pytypes-action@main
      with:
        args: -d import-error